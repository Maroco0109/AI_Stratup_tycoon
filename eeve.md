업데이트: Day 1 프롬프트 하드코딩 및 EEVE 실패 원인 분석

요약
- Day 1 프롬프트를 하드코딩: 모델 호출 없이 아래 문구를 그대로 표시합니다.
  - "창의적인 AI기반 사업 아이디어를 제시해주세요!"
  - 권장: 한 문단, 한국어 존댓말
- EEVE가 답변을 제대로 생성하지 못한 원인을 정리하고, 개선책을 제시합니다.

핵심 변경 사항(코드)
- engine.get_event_card(day)
  - day == 1일 때 고정 event_card를 반환(모델 호출 생략).
  - response_instructions는 "창의적인 AI기반 사업 아이디어를 제시해주세요!"로 고정.
  - constraints에 한 문단·존댓말을 명시, eval_focus는 창의성/현실성/리스크로 간결화.
- app.render_input_help(day)
  - Day 1 도움말도 동일 문구를 직접 노출하여 EEVE/GPT-4o-mini 모두 동일한 사용자 경험 제공.
- prompts/rulebook.md
  - EEVE 전용 규칙을 강화(단일 fenced JSON, 반복/재서술 금지, 키·타입 제한).

EEVE 답변 실패 원인(분석)
1) JSON-only 규칙과 로컬 모델의 서사/반복 경향 충돌
   - STRICT_JSON=True에서 EEVE가 머리/꼬리 문장, 메타 설명, 키 누락을 일으켜 파싱 실패 빈발.
   - 조치: EEVE 섹션에 "단 하나의 fenced JSON"/"반복·재서술 금지"/"허용 키·타입 한정"을 명문화.

2) 인코딩(모지바케)로 인한 지시 손실 가능성
   - 일부 시스템/유저 문자열이 깨진 한글로 저장되어 모델이 핵심 규칙을 정확히 해석하지 못할 수 있음.
   - 조치 제안: 소스 전체 UTF-8 일관 저장, 요청 페이로드는 유니코드 그대로 전달(ensure_ascii=False 유지), 연쇄 테스트로 한글 지시가 원문대로 보이는지 확인.

3) 프롬프트 과복잡(룰/루브릭/예시 중첩)
   - 규칙이 길고 중첩될수록 로컬 모델이 핵심 요구(“JSON만”)를 놓치기 쉬움.
   - 조치: Day 1은 하드코딩으로 단순화, 나머지 Day 2~6은 EEVE 허용 키를 축소해 선택지 최소화.

4) 샘플링 하이퍼파라미터 영향
   - TEMPERATURE가 높으면 서사/중복/비JSON 출력 증가.
   - 권장 초기값: temperature≈0.2(±0.1), top_p≈0.9, repeat_penalty≈1.1~1.2. 모델별 최적값은 config에서 조정.

5) 파싱/에러 핸들링 경로
   - fenced JSON 미존재 시 _safe_json 예외 → UI 공란. 개발 모드에선 STRICT_JSON=False로 가변 수용→로깅→수정 루프 권장.

검증 체크리스트
- [ ] Day 1: UI에 고정 문구가 그대로 노출되는지(모델 무관).
- [ ] Day 2~6: EEVE 출력이 단일 fenced JSON인지, 허용 키/타입 제약을 준수하는지.
- [ ] EEVE 출력에 점수/등급/수치(delta/score) 언급이 전혀 없는지.
- [ ] OpenAI(Tester)만 delta/score를 산출하는지.

권장 운영 팁
- 모델 응답 실패 시 로그 원문(raw) 저장으로 패턴을 수집하고, 규칙/키 제약/샘플링 값을 점진 조정.
- Day 1을 하드코딩함으로써 초기 입력 품질을 안정화하고, 이후 일자의 판정/채점 품질을 높입니다.

